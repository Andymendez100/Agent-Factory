# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/agent_factory
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=agent_factory

# Redis
REDIS_URL=redis://redis:6379/0

# OpenAI
OPENAI_API_KEY=sk-your-key-here

# Encryption (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEY=your-fernet-key-here

# SMTP (for send_alert tool — optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Mock Portal
MOCK_PORTAL_URL=http://mock-portal:8001
MOCK_PORTAL_USERNAME=admin
MOCK_PORTAL_PASSWORD=demo123
